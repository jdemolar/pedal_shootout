# PostgreSQL connection (local development)
DATABASE_URL=postgresql://pedal_shootout_app:localdev@localhost:5432/pedal_shootout

# MongoDB Realm (legacy — used by React frontend until API migration)
REACT_APP_REALM_APP_ID=your-realm-app-id

# Spring Boot API (local development)
REACT_APP_API_BASE_URL=http://localhost:8081
